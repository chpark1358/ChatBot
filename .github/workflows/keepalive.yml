name: supabase-keepalive

on:
  schedule:
    - cron: "0 0 */3 * *"   # 3일마다 실행
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase Project 1
        run: |
          curl -s https://rkoxmzaaxalentpgygfv.supabase.co

      - name: Ping Supabase Project 2
        run: |
          curl -s https://daomdiicbhtdjcchemnl.supabase.co
